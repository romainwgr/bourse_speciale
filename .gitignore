# Ignorer node_modules
node_modules/

# Ignorer les fichiers de log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignorer .env si tu utilises des variables d'environnement sensibles
# On l'ignore pas pour l'instant car flm de le reload chaque fois
# Autres fichiers spécifiques à ignorer, si nécessaire
dist/

.DS_Store